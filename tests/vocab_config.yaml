# Test config: single OpenSearch backend, one index per vocabulary
# (alias concepts_<identifier>, derived from the identifier).
vocabularies:
  - identifier: jel
    type: local_os
    config:
      host: http://localhost
      port: 9200

  - identifier: mesh
    type: local_os
    config:
      host: http://localhost
      port: 9200

  - identifier: acm
    type: local_os
    config:
      host: http://localhost
      port: 9200
